<!-- <%= audio_tag('your_childhood_song.mp3', controls: true) %> -->
<audio src="http://199.180.75.58:9448/stream" controls crossorigin='anonymous'></audio>
<script>
    AudioContext = window.AudioContext || window.webkitAudioContext;
    audioCtx = new AudioContext();
    audioElement = document.querySelector('audio');
    track = audioCtx.createMediaElementSource(audioElement);
    track.connect(audioCtx.destination);
</script>

<%= react_component('FullScreenVisualizer',
      props: {v_shader: vertex_shader,
        f_shader: fragment_shader,
        particleCount: 1000000}) %>